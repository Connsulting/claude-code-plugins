{
  "description": "TEMPORARILY DISABLED 2026-08-06: auto-peek (UserPromptSubmit) and auto-extract (SessionEnd/PreCompact) removed. Restore with: cp hooks.json.enabled-backup hooks.json. Codex-side twins are commented out in ~/.codex/config.toml.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/setup.sh",
            "timeout": 120000
          }
        ]
      }
    ]
  }
}
