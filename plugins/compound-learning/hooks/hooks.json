{
  "description": "Compound-learning hooks are temporarily disabled. Automatic peek and extraction were paused on 2026-08-06; the SessionStart bootstrap was paused on 2026-08-14 because it installed a 4.6 GB CUDA PyTorch stack on CPU-only hosts. Restore only the bootstrap from hooks.json.bootstrap-backup, or restore all hooks from hooks.json.enabled-backup.",
  "hooks": {}
}
